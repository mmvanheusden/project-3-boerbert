{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test Backend",
            "type": "bun",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "src/index.ts",
            "watchMode": true
        }
    ]
}